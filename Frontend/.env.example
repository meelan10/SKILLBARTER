# Backend API base (matches backend PORT=5000, mounted under /api)
VITE_API_URL="http://localhost:5000/api"

# Socket.IO server (same backend, root URL — not under /api)
VITE_SOCKET_URL="http://localhost:5000"
